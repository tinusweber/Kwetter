name: Docker Image CI

on:
  push:
    branches: [ "main" ]
    paths:
      - Backend/**

jobs:

  build_TweetService:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        cd Backend
        docker build . --file ./TweetService/TweetService.Api/Dockerfile --tag ghcr.io/tinusweber/Kwetter/tweetservice:latest
        echo $CR_PAT | docker login ghcr.io -u ${{ github.actor }} -p ${{secrets.GITHUB_TOKEN}}
        cd TweetService/TweetService.Api
        docker push ghcr.io/tinusweber/Kwetter/tweetservice:latest
  build_ProfileService:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        cd Backend
        docker build . --file ./ProfileService/ProfileService.Api/Dockerfile --tag ghcr.io/tinusweber/Kwetter/profileservice:latest
        echo $CR_PAT | docker login ghcr.io -u ${{ github.actor }} -p ${{secrets.GITHUB_TOKEN}}
        cd ProfileService
        docker push ghcr.io/tinusweber/Kwetter/profileservice:latest
  build_AuthService:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        cd Backend
        docker build . --file ./Auth/AuthService.Api/Dockerfile --tag ghcr.io/tinusweber/Kwetter/authservice:latest
        echo $CR_PAT | docker login ghcr.io -u ${{ github.actor }} -p ${{secrets.GITHUB_TOKEN}}
        docker push ghcr.io/tinusweber/Kwetter/authservice:latest
  build_PostScanner:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        cd Backend
        docker build . --file ./PostScanner/PostScanner/Dockerfile --tag ghcr.io/tinusweber/Kwetter/postscanner:latest
        echo $CR_PAT | docker login ghcr.io -u ${{ github.actor }} -p ${{secrets.GITHUB_TOKEN}}
        docker push ghcr.io/tinusweber/Kwetter/postscanner:latest
  build_Gateway:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        cd Backend
        docker build . --file ./Gateway/Dockerfile --tag ghcr.io/tinusweber/Kwetter/gateway:latest
        echo $CR_PAT | docker login ghcr.io -u ${{ github.actor }} -p ${{secrets.GITHUB_TOKEN}}
        docker push ghcr.io/tinusweber/Kwetter/gateway:latest
  build_ReportService:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        cd Backend
        docker build . --file ./ReportService/ReportService.Api/Dockerfile --tag ghcr.io/tinusweber/Kwetter/reportservice:latest
        echo $CR_PAT | docker login ghcr.io -u ${{ github.actor }} -p ${{secrets.GITHUB_TOKEN}}
        docker push ghcr.io/tinusweber/Kwetter/reportservice:latest
  build_CommentService:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        cd Backend
        docker build . --file ./CommentService/CommentService.Api/Dockerfile --tag ghcr.io/tinusweber/Kwetter/commentservice:latest
        echo $CR_PAT | docker login ghcr.io -u ${{ github.actor }} -p ${{secrets.GITHUB_TOKEN}}
        docker push ghcr.io/tinusweber/Kwetter/commentservice:latest
