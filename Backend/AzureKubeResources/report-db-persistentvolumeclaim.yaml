apiVersion: v1
kind: PersistentVolumeClaim
metadata:

  labels:
    io.kompose.service: report-db
  name: report-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
